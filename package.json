{
  "name": "regular-radiation",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-images": "node scripts/generate-images.cjs",
    "watch-images": "chokidar \"src/assets/**/*\" -c \"npm run generate-images\""

  },
  "dependencies": {
    "astro": "^6.1.8",
    "sass": "^1.99.0"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0"
  }
}
